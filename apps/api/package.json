{
  "name": "@sitepilot/api",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/server.ts",
    "build": "tsc",
    "start": "bun run src/server.ts",
    "lint": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@hono/zod-validator": "^0.4.0",
    "@sitepilot/db": "workspace:*",
    "@sitepilot/shared": "workspace:*",
    "bcryptjs": "^3.0.3",
    "hono": "^4.6.0",
    "jsonwebtoken": "^9.0.3",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/bcryptjs": "^3.0.0",
    "@types/jsonwebtoken": "^9.0.10",
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0"
  }
}
